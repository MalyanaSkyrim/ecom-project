meta {
  name: Delete Product
  type: http
  seq: 5
}

delete {
  url: {{API_URL}}/v1/products/:id
  body: none
  auth: bearer
}

params:path {
  id: product-id-here
}

auth:bearer {
  token: {{API_KEY}}
}

